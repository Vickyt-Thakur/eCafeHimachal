import { useEffect, useState } from 'react';
import { api } from '../services/api.js';
export default function AdminCustomersPage() { const [customers, setCustomers] = useState([]); const [message, setMessage] = useState('Loading customers…'); useEffect(() => { api.get('/admin/customers').then(({ data }) => { setCustomers(data.customers); setMessage(''); }).catch((error) => setMessage(error.response?.data?.message || 'Unable to load customers.')); }, []); return <main className="p-5 sm:p-10"><h1 className="text-3xl font-bold text-slate-900">Customers</h1><p className="mt-2 text-slate-600">Customer accounts registered on eCafeHimachal.</p>{message && <p className="mt-5 text-slate-600">{message}</p>}<section className="mt-7 overflow-hidden rounded-xl border border-slate-200 bg-white"><div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="bg-slate-50 text-slate-500"><tr><th className="px-5 py-3">Name</th><th className="px-5 py-3">Mobile</th><th className="px-5 py-3">Email</th><th className="px-5 py-3">Status</th><th className="px-5 py-3">Joined</th></tr></thead><tbody>{customers.map((customer) => <tr key={customer.id} className="border-t border-slate-100"><td className="px-5 py-4 font-semibold">{customer.name}</td><td className="px-5 py-4">{customer.mobile}</td><td className="px-5 py-4">{customer.email || '-'}</td><td className="px-5 py-4">{customer.status}</td><td className="px-5 py-4">{new Date(customer.createdAt).toLocaleDateString('en-IN')}</td></tr>)}{customers.length === 0 && !message && <tr><td colSpan="5" className="px-5 py-8 text-center text-slate-500">No customers yet.</td></tr>}</tbody></table></div></section></main>; }
