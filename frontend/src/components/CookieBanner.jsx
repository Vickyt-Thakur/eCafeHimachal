import { useState } from 'react';
import { Link } from 'react-router-dom';
export default function CookieBanner() { const [visible, setVisible] = useState(() => !localStorage.getItem('ecafe-cookie-choice')); if (!visible) return null; function choose(value) { localStorage.setItem('ecafe-cookie-choice', value); setVisible(false); } return <section role="dialog" aria-label="Cookie preferences" className="fixed inset-x-4 bottom-4 z-50 mx-auto max-w-3xl rounded-2xl border border-slate-200 bg-white p-5 shadow-2xl"><p className="font-bold text-slate-900">Cookie preferences</p><p className="mt-2 text-sm leading-6 text-slate-600">We use cookies and similar technologies as described in our <Link className="font-semibold text-[#276052] underline" to="/cookie-policy">Cookie Policy</Link>.</p><div className="mt-4 flex flex-wrap gap-3"><button onClick={() => choose('accepted')} className="rounded-full bg-[#143f3a] px-4 py-2 text-sm font-bold text-white">Accept cookies</button><button onClick={() => choose('essential-only')} className="rounded-full border border-slate-300 px-4 py-2 text-sm font-bold text-slate-700">Essential only</button></div></section>; }
